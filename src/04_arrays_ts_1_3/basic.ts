const bucketList: string [] = ["Buenos Aires", "Iceland", "Amsterdam"];
const luckyNumbers: number [] = [27, 77, 111];
const favoritePeople: string [] = ["Mama", "Björn", "Oma"];

console.log(bucketList);
console.log(luckyNumbers);
console.log(favoritePeople);

const bucketLength = bucketList.length;
const numbersLength = bucketList.length;
const favouretLength = bucketList.length;
console.log(bucketLength);
console.log(numbersLength);
console.log(favouretLength);
